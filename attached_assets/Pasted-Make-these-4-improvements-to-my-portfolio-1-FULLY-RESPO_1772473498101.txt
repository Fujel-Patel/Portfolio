Make these 4 improvements to my portfolio:

=== 1. FULLY RESPONSIVE — ALL PAGES ===
Make every section (Home, About, Projects, Contact) responsive:
- Mobile (< 768px): single column, smaller fonts, stacked layout
- Tablet (768–1024px): adjusted grid
- Desktop (> 1024px): full layout
- Use CSS media queries or Tailwind responsive classes
- Navbar: hamburger menu on mobile
- 3D model: smaller on mobile, hidden on very small screens

=== 2. SINGLE PAGE SECTIONS — NO CONTENT OVERLAP ===
Each section must show ONLY its own content when navigated to:
- Use React Router OR anchor-based smooth scroll
- Each section: min-height 100vh
- When on Home: only hero visible in viewport
- When on About: only about content visible
- When on Projects: only projects visible
- When on Contact: only contact visible
- Active nav link highlights based on current section
- Use IntersectionObserver to detect which section is active

=== 3. SMOOTH PAGE TRANSITIONS ===
Install and use Framer Motion for section transitions:

npm install framer-motion

- Wrap each section/page with:
  <motion.div
    initial={{ opacity: 0, y: 60 }}
    animate={{ opacity: 1, y: 0 }}
    exit={{ opacity: 0, y: -60 }}
    transition={{ duration: 0.6, ease: "easeInOut" }}
  >

- Add scroll-behavior: smooth to CSS
- Navbar clicks scroll smoothly to sections with 
  easing animation
- Section entrance: fade up animation when scrolled into view
- Use whileInView prop for scroll-triggered animations

=== 4. PROJECTS — ADD REAL LINKS ===
Update projects data with these exact links:

Project 1: Real-Time Chat Application
- Live Demo: https://chat-webapplication.vercel.app (or current Vite+React link)
- GitHub: https://github.com/Fujel-Patel/Chat-WebApplication
- Description: Chat app using MERN stack, Socket.IO, JWT auth

Project 2: Food Delivery App  
- Live Demo: https://meal-app.vercel.app (or current Vite+React link)
- GitHub: https://github.com/Fujel-Patel/Meal
- Description: Food delivery app with Redux, Tailwind CSS

Each project card must have:
- "Live Demo" button → opens in new tab (_blank)
- "GitHub" button → opens in new tab (_blank)  
- Project thumbnail/preview image
- Tech stack tags (React, Node.js, MongoDB etc.)
- Hover effect: card lifts up with glow

=== IMPORTANT ===
- Do not break existing 3D hero model
- Do not change color theme
- Keep all existing content/data same
- Only add responsiveness, transitions, and project links